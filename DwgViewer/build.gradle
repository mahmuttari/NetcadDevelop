// DWG OfficeZip – kök yapı dosyası
plugins {
    id "com.android.application" version "8.11.1" apply false
    // Kotlin ve Compose YALNIZ açılış ekranı için: başvuru sahibinin gönderdiği Jetpack Compose
    // ekranı (com.example.dwgloader) olduğu gibi, tek satırı değiştirilmeden derlenebilsin diye.
    id "org.jetbrains.kotlin.android" version "2.0.21" apply false
    id "org.jetbrains.kotlin.plugin.compose" version "2.0.21" apply false
}
